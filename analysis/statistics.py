"""
Reserved for cross-video / cross-group statistical comparisons (e.g. t-tests
or ANOVA between experimental groups using the Summary sheets that
output/excel.py writes per video).

Nothing here yet -- this is scaffolding for a future step, not a step-1
deliverable. BatchSummary.csv (written by gui/main_window.py's run_batch)
is the current source of per-video summary rows to build this from.
"""
