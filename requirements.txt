opencv-python>=4.8
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1
scipy>=1.10
tqdm>=4.65

# tkinter is part of the Python standard library, not a pip package.
# It ships with the standard Windows/macOS Python installers.
# On Linux, if "import tkinter" fails, install it via your system
# package manager, e.g.:  sudo apt-get install python3-tk
